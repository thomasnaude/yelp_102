<h1>New review for <%= restaurant.name %></h1>

<%= simple_form_for [restaurant, review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: Review.possible_ratings %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
